version: 0.2
phases:
  build:
    commands:
      - mvn clean package

artifacts:
  files:
    - target/pinoypoetry-1.0.0.war
    - appspec.yml
    - scripts/start.sh
    - scripts/stop.sh
cache:
  paths:
  - '/root/.m2/**/*'